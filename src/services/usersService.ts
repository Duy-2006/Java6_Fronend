/*
 * usersService.ts
 * Lop service xu ly cac thao tac CRUD doi voi tai khoan nguoi dung (User/Admin).
 * Hien tai cac ham chua duoc cai dat (placeholder) - se bo sung khi can.
 */

// Doi tuong gom nhom cac ham xu ly user - chua duoc cai dat day du
export const usersService = {
  getAll: async () => {},              // Lay toan bo danh sach nguoi dung
  getById: async (id: string) => {},   // Lay thong tin 1 nguoi dung theo ID
  create: async (data: any) => {},     // Tao tai khoan nguoi dung moi
  update: async (id: string, data: any) => {},  // Cap nhat thong tin nguoi dung
  delete: async (id: string) => {},    // Xoa tai khoan nguoi dung
};
